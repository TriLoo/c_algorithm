/**
 * @param n 输入的参数， n = (rows * rows - 1)
 *
 * @brief 思路
 *     首先生成一个符合题目要求的二维矩阵，然后打印该矩阵。
 *
 *     矩阵的生成过程，可以分为四个步骤：
 *          从左到右生成一行，从上到下生成一列，从右到左生成一行，从下到上生成一列。
 *
 *     空间复杂度：O(n+1), 一个rows * rows的二维矩阵
 *     时间复杂度：O(n)
 *
 *
 *     改进方法：
 *          可改进空间复杂度到O(rows)或更低，但时间复杂度会逐渐变成O(n^2)。
 */
